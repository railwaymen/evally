default: &default
  database:
    host: localhost
    username: postgres
    password:

development:
  host: http://localhost:3000
  auth_mailer: 'Evally <no-reply@example.com>'

test:
  host: http://testhost
  auth_mailer: 'Evally <no-reply@example.com>'
