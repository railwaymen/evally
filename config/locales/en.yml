en:
  activerecord:
    errors:
      format: '%{attribute} %{message}'
      messages:
        draft_exists: 'draft evaluation already exists'
        invalid_state: "'%{value}' is not a valid value"
        name_and_value: 'must have name and value'
        skills_array: 'must be an Array of skills'
        has_draft_evaluations: 'has draft evaluations'
