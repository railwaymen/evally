.landing-form {
  width: 90%
  position: absolute
  top: 40%
  left: 50%
  transform: translate(-50%, -40%)
  background: #fff
  box-shadow: 0 .5rem 1rem rgba($black, .25)
  padding: 4rem 5rem
  border-radius: .5rem

  +respond(sm) {
    width: 42rem
  }

  &__logo {
    width: 15rem
    height: auto
    margin: 3rem auto
    position: relative

    img {
      width: 100%
      height: auto
    }
  }

  &__heading {
    font-size: 3.6rem
    font-weight: 600

    &--dark {
      color: $font-gray
    }
  }

  &__question {
    font-size: 1.6rem
    text-align: center
    padding: 1rem

    span {
      transition: all .2s

      &:hover {
        text-decoration: underline
        cursor: pointer
      }
    }
  }
}